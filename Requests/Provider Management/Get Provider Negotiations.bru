meta {
  name: Get Provider Negotiations
  type: http
  seq: 1
}

post {
  url: {{PROVIDER_CP}}/api/mgmt/v4/contractnegotiations/request
  body: json
  auth: inherit
}

body:json {
  {}
}

assert {
  res.status: eq 200
}

tests {
  test("Status code is 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response is an array of provider contract negotiations", function() {
    expect(res.getBody()).to.be.an('array');
  });
}

