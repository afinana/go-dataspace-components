meta {
  name: List Consumer Transfers
  type: http
  seq: 12
}

post {
  url: {{CONSUMER_CP}}/api/consumer/v4/transferprocesses/request
  body: json
  auth: inherit
}

body:json {
  {}
}

script:post-response {
  test("Status code is >=200 and <300", function () {
      expect(res.getStatus() < 300 && res.getStatus() >= 200).to.be.true
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
